<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="ChefsForSeniors.Views.ListWeeksPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fe="clr-namespace:FreshEssentials;assembly=FreshEssentials"
             xmlns:local="clr-namespace:ChefsForSeniors.Views;assembly=ChefsForSeniors"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             Title="{Binding Path=Title}"
             prism:ViewModelLocator.AutowireViewModel="True">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackLayout Grid.Row="0">
            <Label Text="Client name" />
            <Entry Text="{Binding Client.Name, Mode=TwoWay}" />
            <Button Command="{Binding SaveCommand}" Text="Save client" />
            <Button Command="{Binding DeleteCommand}" Text="Delete client" />
        </StackLayout>

        <c:ListViewEx xmlns:c="ChefsForSeniors.Controls"
                      Grid.Row="1"
                      fe:ListViewItemTappedAttached.Command="{Binding Path=SelectedCommand}"
                      Header="{Binding Path=Title}"
                      ItemsSource="{Binding Path=Items}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Detail="{Binding Path=Detail}" Text="{Binding Path=Text}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </c:ListViewEx>

        <Button Grid.Row="2"
                Command="{Binding NewCommand}"
                Text="+ Week" />

    </Grid>

</ContentPage>

